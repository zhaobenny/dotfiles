# benny's dotfiles

Run `./install.sh` for first-time setup or to install new packages. 

Dotfiles auto-update from git periodically.

Machine-specific shell helpers live in `~/.bash_aliases.local` and stay untracked.
